export default function Header({ updatedAt, onRun, running }) {
  return <header className="app-header"><div><div className="brand"><span className="brand-mark">CY</span><span>02</span></div><div className="kicker">GRAPH-AWARE VULNERABILITY PRIORITIZATION</div><h1>Patch what changes critical exposure.</h1><p className="header-context">40-host undirected development instance · entries 0, 1 · critical assets 35–39</p></div><div className="header-actions"><span className="updated">{updatedAt ? `UPDATED ${new Date(updatedAt).toLocaleTimeString()}` : 'DETERMINISTIC ANALYSIS'}</span><button className="primary-button" onClick={onRun} disabled={running}>{running ? 'Running...' : 'Run analysis'}</button></div></header>;
}
