import { ResponsiveContainer, ScatterChart, Scatter, XAxis, YAxis, CartesianGrid, Tooltip } from 'recharts';

export default function RiskChart({ items }) { const data = items.map(item => ({ name: item.vulnerability_id, priority: Number(item.priority_score), patch: Number(item.patch_value), host: item.host_id })); return <section className="panel chart-panel"><div className="panel-head"><div><span className="kicker">DECISION SIGNAL</span><h2>Priority vs patch impact</h2></div><span className="panel-note">each dot is a finding</span></div><div className="chart"><ResponsiveContainer width="100%" height="100%"><ScatterChart margin={{ top: 12, right: 20, bottom: 20, left: 0 }}><CartesianGrid stroke="#2a4944" strokeDasharray="3 3" /><XAxis type="number" dataKey="priority" name="Priority" unit="" stroke="#8daaa0" /><YAxis type="number" dataKey="patch" name="Patch value" stroke="#8daaa0" /><Tooltip cursor={{ strokeDasharray: '3 3' }} contentStyle={{ background: '#102a27', border: '1px solid #42655d' }} /><Scatter name="Findings" data={data} fill="#e0a85e" /></ScatterChart></ResponsiveContainer></div></section>; }
